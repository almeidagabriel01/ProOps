---
phase: 03-proposals-crm-e2e
task: gap-closure
total_tasks: gap-closure
status: in_progress
last_updated: 2026-04-07T16:40:23.597Z
---

<current_state>
Phase 03 executed and verified with gaps_found (3/4 must-haves). PROP-04, PROP-05, PROP-06 pass. PROP-01/02/03 CRUD tests have two remaining issues being debugged outside of GSD plan structure directly in the E2E test files.

We are mid-debug on the PROP-01/02/03 failures. The work so far is committed to `develop`. No uncommitted code changes (only build artifacts and test results).
</current_state>

<completed_work>

- Phase 03 plans 03-01 and 03-02 executed by gsd-executor agents ✓
- PROP-04 (PDF auth enforcement), PROP-05 (public share link), PROP-06 (status transitions) all pass ✓
- `global-setup.ts`: added `npm run build` for functions before starting emulator (commit `99432d9d`) ✓
- `e2e/seed/data/products.ts`: seeded 1 product (`product-sensor-001`) for tenant-alpha (commit `92350497`) ✓
- `e2e/seed/seed-factory.ts`: wired products seed into seedAll/clearAll ✓
- **PROP-02 now passes** — product seed resolved the `isLoading` stuck issue in `useProposalForm.loading-effects.ts`
</completed_work>

<remaining_work>

**PROP-01 still failing** — `createProposal` reaches step 5 (Resumo), clicks "Criar Proposta", but no redirect happens within 20s timeout. Root cause: `ProductService.getProducts()` now returns 1 product on the FIRST test run, triggering a React re-render via `setProducts()` that occurs mid-submission and disrupts the form navigation. Attempts:
- `waitForLoadState("networkidle")` — reverted; Firestore WebSocket prevents networkidle from ever settling
- Next approach: add explicit `waitForTimeout(1500)` before submit to let the product fetch settle, OR investigate whether the re-render resets form state (check `useProposalForm.core.ts` effects that depend on `products`), OR intercept the network response for the POST to see if there's an API error.

**PROP-03 still failing** — `deleteProposal()` POM correctly finds the compact dropdown row, clicks "Mais ações" → "Excluir" → AlertDialog confirm, but proposal remains visible. `handleDelete` likely catches an API error (silent failure). Root cause unknown. To investigate: add `page.on('response')` listener in the POM, or check browser console for error toasts. PROP-03 may self-heal once PROP-01 is fixed (PROP-01 currently leaves an uncleaned proposal that could be affecting state).
</remaining_work>

<decisions_made>

- Seeding a product (`product-sensor-001`) in `e2e/seed/data/products.ts` rather than changing application code (`useProposalForm.loading-effects.ts` guard `products.length === 0`). Keeps E2E infrastructure responsible for test setup.
- `networkidle` approach was tried and reverted — Firestore uses persistent WebSocket so `networkidle` never settles within reasonable time.
- The `products` seed collection was added to `clearAll` in `seed-factory.ts` to ensure clean state on each run.
</decisions_made>

<blockers>

- **PROP-01 race condition**: Product fetch triggers React re-render during form submission on first test run. Simple `waitForTimeout(500)` may not be enough; need to either (a) verify what the API returns after submit (network log), (b) check if the re-render resets any critical state in `useProposalForm`, or (c) wait for the submit button to not be disabled (indicating no pending re-renders).
- **PROP-03 delete failure**: Silent API failure — need to inspect what `ProposalService.deleteProposal()` actually throws. The handler catches the error but it's not visible in the test output. Could be a Functions emulator issue, auth token issue, or the delete request going to wrong URL.
</blockers>

<context>
All changes are on `develop` branch. The phase 03 VERIFICATION.md exists and marks `gaps_found` (PROP-01/02/03 failing). Once PROP-01/02/03 pass, re-run gsd-verifier to produce `passed` status and then mark phase complete.

The investigation has been manual (no new GSD plans created). This is gap-closure work outside of plan structure, directly debugging the E2E test locators and race conditions.

Key files changed:
- `e2e/global-setup.ts` — build functions before emulator start
- `e2e/seed/data/products.ts` — new file, 1 product seed
- `e2e/seed/seed-factory.ts` — import + call seedProducts, add to clearAll
- `e2e/pages/proposals.page.ts` — currently at original state (networkidle revert committed)
</context>

<next_action>
1. Run `npm run test:e2e -- --grep "PROP-01"` to get a clean baseline of PROP-01's failure
2. Check PROP-01's error context screenshot/snapshot to confirm the Resumo step is reached and "Criar Proposta" is clicked
3. Add Playwright `page.on('response', ...)` listener in the test fixture or in `createProposal` to capture the POST /v1/proposals response status — if 4xx/5xx, that's the API error
4. OR try `waitForTimeout(2000)` instead of 500ms before submit click (enough for 1-product fetch to settle)
5. After PROP-01 fixed, check if PROP-03 self-heals (PROP-01 cleanup removes leftover proposal)
6. Once all 3 pass: re-run `/gsd:verify-work 03` to update VERIFICATION.md → phase complete
</next_action>
