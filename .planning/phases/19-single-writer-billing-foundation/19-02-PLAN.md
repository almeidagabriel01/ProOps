---
phase: 19-single-writer-billing-foundation
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - apps/functions/src/stripe/stripeWebhook.ts
  - apps/functions/src/billing/__tests__/syncTenantPlanBillingSnapshot.test.ts
autonomous: true
requirements: [BILL-06]
tags: [billing, stripe, single-writer, transaction]

must_haves:
  truths:
    - "syncTenantPlanBillingSnapshot accepts the full SyncTenantPlanBillingSnapshotParams (every field used by current parallel writers)"
    - "Inside the existing db.runTransaction(), the function writes BOTH top-level fields AND a subscription.* nested map atomically"
    - "All three in-file webhook handlers (handleSubscriptionUpdated deferral path, handleSubscriptionDeleted, handleInvoicePaymentFailed) call the function instead of writing tenantRef directly"
    - "whatsappEnabled remains a SECOND write OUTSIDE the transaction (Pitfall 2 — do not move inside)"
    - "syncTenantPlanBillingSnapshot is EXPORTED as a named export so Plan 03 (controllers, billing-sync, stripeHelpers) can import it"
    - "beginStripeEventProcessing is EXPORTED as a named export (signature unchanged) so Plan 05's emulator replay test can call it twice with the same eventId without a fallback path"
  artifacts:
    - path: "apps/functions/src/stripe/stripeWebhook.ts"
      provides: "Extended syncTenantPlanBillingSnapshot writing top-level + subscription.* atomically; consolidated in-file handlers; named exports for syncTenantPlanBillingSnapshot AND beginStripeEventProcessing"
      contains: "subscription:"
      contains: "lastEventId"
      contains: "export async function beginStripeEventProcessing"
    - path: "apps/functions/src/billing/__tests__/syncTenantPlanBillingSnapshot.test.ts"
      provides: "Real assertions replacing Plan 01 test.todo scaffolds"
      contains: "expect"
  key_links:
    - from: "apps/functions/src/stripe/stripeWebhook.ts (syncTenantPlanBillingSnapshot)"
      to: "tenants/{tenantId}.subscription nested map"
      via: "transaction.set(tenantRef, { subscription: {...} }, {merge:true})"
      pattern: "subscription:\\s*\\{"
    - from: "handleSubscriptionUpdated/Deleted/InvoicePaymentFailed (same file)"
      to: "syncTenantPlanBillingSnapshot"
      via: "direct function call"
      pattern: "syncTenantPlanBillingSnapshot\\("
    - from: "Plan 05 stripe-idempotency.test.ts"
      to: "stripeWebhook.ts beginStripeEventProcessing (named export)"
      via: "import { beginStripeEventProcessing } from \"../../stripe/stripeWebhook\""
      pattern: "export\\s+async\\s+function\\s+beginStripeEventProcessing"
---

<objective>
Extend `syncTenantPlanBillingSnapshot` in `apps/functions/src/stripe/stripeWebhook.ts` so that it (a) accepts the full canonical parameter shape declared in Plan 01, (b) writes the `subscription.*` nested map alongside the existing top-level fields inside the SAME `db.runTransaction()`, and (c) is the sole writer used by every in-file webhook handler. Replace the Wave 0 test scaffold with real emulator assertions. Also promote `beginStripeEventProcessing` to a named export (signature unchanged) so Plan 05 can drive it directly from the BILL-08 emulator replay test without a fallback path.

This plan does NOT touch `stripe.controller.ts`, `billing-sync.service.ts`, `stripeHelpers.ts`, or `applyScheduledPlanChanges.ts` — those external consolidations are Plan 03. This plan also does NOT touch the LRU caches — that is Plan 04. By scoping to a single file we avoid file-ownership conflicts that would force serialized waves.

**CRITICAL Pitfall 2:** `whatsappEnabled` MUST remain a SECOND `tenantRef.update({ whatsappEnabled })` OUTSIDE the transaction. `tenantPlanAllowsWhatsApp()` reads addon docs and the plan cache — moving it inside the transaction causes lock contention and disallowed external reads.

**CRITICAL — Top-level fields stay:** Per the reader audit in 19-RESEARCH.md, EVERY top-level billing field has active readers; `subscriptionStatus` and `currentPeriodEnd` are Firestore query filters in `checkManualSubscriptions.ts`. Write to BOTH top-level AND `subscription.*`. NEVER drop a top-level field in this phase.

**BILL-08 export decision (USER-LOCKED):** Per the user's revision decision, `beginStripeEventProcessing` is promoted to a named export in this plan (Task 2). The function signature, behavior, and internal `db.runTransaction()` are NOT modified — only its visibility changes. Plan 05's BILL-08 test imports it and calls it twice with the same synthetic eventId; first call → "process", second call → "skip", with no Firestore mutation between snapshots.
</objective>

<execution_context>
@D:/DEV/ProOps/.claude/get-shit-done/workflows/execute-plan.md
@D:/DEV/ProOps/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/DEV/ProOps/.planning/PROJECT.md
@D:/DEV/ProOps/.planning/STATE.md
@D:/DEV/ProOps/.planning/phases/19-single-writer-billing-foundation/19-CONTEXT.md
@D:/DEV/ProOps/.planning/phases/19-single-writer-billing-foundation/19-RESEARCH.md
@D:/DEV/ProOps/.planning/phases/19-single-writer-billing-foundation/19-01-SUMMARY.md
@D:/DEV/ProOps/apps/functions/CLAUDE.md
@D:/DEV/ProOps/apps/functions/src/CLAUDE.md
@D:/DEV/ProOps/.claude/rules/backend.md
@D:/DEV/ProOps/.claude/rules/security.md

<interfaces>
<!-- Function being extended (apps/functions/src/stripe/stripeWebhook.ts ~lines 129-196). -->
<!-- Currently EXPORTED? No — file-private. Plan 02 makes it `export async function`. -->

```typescript
// CURRENT signature/body (excerpt — read full file lines 129-196):
async function syncTenantPlanBillingSnapshot(params: {
  tenantId: string;
  subscriptionStatus: string;
  stripePriceId?: string;
  clearScheduled?: boolean;
  currentPeriodEnd?: Date;
}): Promise<void> {
  const tenantRef = db.collection("tenants").doc(tenantId);
  const nowIso = new Date().toISOString();
  const derivedTier = params.stripePriceId ? resolvePriceToTier(params.stripePriceId) : null;
  // ...
  await db.runTransaction(async (transaction) => {
    const tenantSnap = await transaction.get(tenantRef);
    const tenantData = tenantSnap.exists ? tenantSnap.data() : undefined;
    const lifecyclePatch = buildTenantSubscriptionLifecyclePatch({...});
    const patch: Record<string, unknown> = {
      ...lifecyclePatch,
      updatedAt: nowIso,
      billingSyncedAt: nowIso,
      ...(params.currentPeriodEnd && { currentPeriodEnd: params.currentPeriodEnd.toISOString() }),
    };
    if (derivedTier) {
      patch.plan = derivedTier;
      if (params.clearScheduled) {
        patch.scheduledPlan = null;
        patch.scheduledPlanAt = null;
        patch.scheduledPlanReason = null;
      }
    }
    transaction.set(tenantRef, patch, { merge: true });
  });
  clearTenantPlanCache(tenantId);
  const allowsWhatsApp = await tenantPlanAllowsWhatsApp(tenantId);
  await tenantRef.update({ whatsappEnabled: allowsWhatsApp });
}
```

<!-- beginStripeEventProcessing — currently file-private (~lines 445-479).
     Plan 02 Task 2 changes ONLY the visibility: `async function` → `export async function`.
     Signature, body, and behavior remain identical. -->

```typescript
// CURRENT (file-private):
async function beginStripeEventProcessing(event: Stripe.Event): Promise<"skip" | "process"> {
  // ... db.runTransaction() with check-and-set on stripe_events/{event.id}
}

// AFTER Plan 02 Task 2 (named export, signature unchanged):
export async function beginStripeEventProcessing(event: Stripe.Event): Promise<"skip" | "process"> {
  // ... unchanged body
}
```

<!-- New params interface from Plan 01: import path = "../shared/billing-types" -->
```typescript
import type { SyncTenantPlanBillingSnapshotParams, SubscriptionSnapshot } from "../shared/billing-types";
```

<!-- In-file callers to consolidate. Search the file for these handler functions and the
     `tenantRef.set(`/`tenantRef.update(` calls inside them. From the writer inventory:
     - handleSubscriptionUpdated deferral writes (scheduledPlan/scheduledPlanAt/cancelAtPeriodEnd/currentPeriodEnd)
     - handleInvoicePaymentFailed (subscriptionStatus past_due, pastDueSince)
     - handleSubscriptionDeleted (subscriptionStatus, plan reset to free, stripePriceId)
     - handleCheckoutCompleted writes trialReservedAt/trialUsedAt only -> EXEMPT (trial writes per CONTEXT.md)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend syncTenantPlanBillingSnapshot to write subscription.* atomically</name>
  <files>apps/functions/src/stripe/stripeWebhook.ts</files>
  <read_first>
    - apps/functions/src/stripe/stripeWebhook.ts (FULL FILE — minimum lines 1-200 plus the handler section that calls syncTenantPlanBillingSnapshot today; identify exact callers before changing the signature)
    - apps/functions/src/shared/billing-types.ts (Plan 01 types — copy field list into the function body)
    - apps/functions/src/lib/tenant-plan-policy.ts lines 297-313 (resolvePriceToTier — confirm signature unchanged)
    - apps/functions/src/billing/__tests__/syncTenantPlanBillingSnapshot.test.ts (Plan 01 scaffold — replace test.todo placeholders)
    - apps/functions/src/stripe/stripeWebhook.ts buildTenantSubscriptionLifecyclePatch (~line 80-127 — used inside; do NOT modify, only invoke)
  </read_first>
  <behavior>
    - Test 1 (RED first): calling extended `syncTenantPlanBillingSnapshot({tenantId, subscriptionStatus:"active", stripePriceId, currentPeriodEnd, eventId, source:"webhook.subscription.updated"})` must result in tenants/{tenantId} having BOTH top-level `subscriptionStatus="active"` AND `subscription.status="active"` after the call. Currently the second is missing.
    - Test 2: `subscription.lastEventId` equals the `eventId` passed in.
    - Test 3: `subscription.syncedAt` is a non-empty ISO string.
    - Test 4: When `clearScheduled=true` AND a tier resolves, BOTH top-level (`scheduledPlan=null`) AND nested (`subscription.scheduledPlan=null`) are cleared.
    - Test 5: When invoked twice in succession with different params, `subscription.*` fields merge (existing fields are preserved when the second call omits them).
    - Test 6: `whatsappEnabled` is written AFTER the transaction completes — verify by spying that `tenantRef.update({whatsappEnabled})` is the last call.
  </behavior>
  <action>
    **Step 1 — Update the signature.** Change the function declaration to:
    ```typescript
    export async function syncTenantPlanBillingSnapshot(
      params: SyncTenantPlanBillingSnapshotParams,
    ): Promise<void>
    ```
    Add the import: `import type { SyncTenantPlanBillingSnapshotParams, SubscriptionSnapshot } from "../shared/billing-types";` (top of file, with other type imports).

    **Step 2 — Inside the existing `db.runTransaction()` callback**, after the existing `const patch` is built, also build a `subscriptionPatch: SubscriptionSnapshot` object containing every field actually provided in `params`, mapped one-to-one:
    - `status` <- params.subscriptionStatus (required, always written)
    - `pastDueSince` <- lifecyclePatch.pastDueSince (already computed; convert null->null)
    - `cancelAtPeriodEnd` <- params.cancelAtPeriodEnd (when provided)
    - `cancelAt` <- params.cancelAt?.toISOString() ?? null (when key present in params)
    - `stripeSubscriptionId` <- params.stripeSubscriptionId (when provided)
    - `stripePriceId` <- params.stripePriceId (when provided)
    - `stripeCustomerId` <- params.stripeCustomerId (when provided)
    - `currentPeriodEnd` <- params.currentPeriodEnd?.toISOString() (when provided)
    - `plan` <- derivedTier ?? params.plan (when either resolves)
    - `scheduledPlan` / `scheduledPlanAt` / `scheduledPlanReason` <- null when clearScheduled+derivedTier; else params.* when provided
    - `syncedAt` <- nowIso (always)
    - `lastEventId` <- params.eventId (when provided)

    Then merge into the patch:
    ```typescript
    const existingSubscription = (tenantData?.subscription as Record<string, unknown> | undefined) ?? {};
    patch.subscription = { ...existingSubscription, ...subscriptionPatch };
    ```
    The single `transaction.set(tenantRef, patch, { merge: true })` call now writes both top-level and nested in one atomic op.

    **Step 3 — Write the top-level passthroughs that current parallel writers use.** Add to the top-level `patch` object (only when the corresponding param is provided — use spread-with-conditional pattern to avoid writing `undefined`):
    - `cancelAtPeriodEnd: params.cancelAtPeriodEnd` (when provided)
    - `cancelScheduledAt: params.cancelScheduledAt` (when provided)
    - `stripeSubscriptionId: params.stripeSubscriptionId` (when provided)
    - `stripeCustomerId: params.stripeCustomerId` (when provided)
    - `priceId: params.stripePriceId` (mirror — billing-sync.service.ts writes both `priceId` and `stripePriceId`; preserve that)
    - `stripePriceId: params.stripePriceId` (when provided)
    - `pastDueSince: lifecyclePatch.pastDueSince` (already in lifecyclePatch — confirm no regression)
    - `trialEndsAt: params.trialEndsAt` (when provided)
    - `billingInterval: params.billingInterval` (when provided)
    - `scheduledPlan / scheduledPlanAt / scheduledPlanReason`: keep current clearScheduled logic; ALSO when caller provides them explicitly (non-null) and clearScheduled is false, persist them.

    DO NOT add any field that isn't in `SyncTenantPlanBillingSnapshotParams` from Plan 01. If you find a parallel writer needs a field not in the type, STOP — surface it for orchestrator approval; do not silently extend the type here.

    **Step 4 — Keep `whatsappEnabled` write OUTSIDE the transaction.** Do not move `clearTenantPlanCache(tenantId)`, `tenantPlanAllowsWhatsApp(tenantId)`, or `tenantRef.update({ whatsappEnabled })` inside `db.runTransaction()`. This is Pitfall 2.

    **Step 5 — Consolidate in-file handlers.** Search this file for direct `tenantRef.set(`/`tenantRef.update(` writes inside (and only inside):
    - `handleSubscriptionUpdated` deferral path (writes scheduledPlan/scheduledPlanAt/cancelAtPeriodEnd/currentPeriodEnd)
    - `handleInvoicePaymentFailed`
    - `handleSubscriptionDeleted`

    Replace each direct write with a `syncTenantPlanBillingSnapshot({...})` call. Pass `eventId: event.id` and `source: "webhook.subscription.updated"` (or matching variant). DO NOT touch `handleCheckoutCompleted` trial writes — they are EXEMPT (per CONTEXT.md trial writes stay direct).

    **Step 6 — Update the test file** (`apps/functions/src/billing/__tests__/syncTenantPlanBillingSnapshot.test.ts`) — replace the 5 `test.todo` placeholders with real `it(...)` tests using the Firebase emulator (or a thin mock of `db` / `transaction` if emulator setup is heavy). Each test maps directly to a behavior listed in `<behavior>` above. Verify GREEN before commit.

    **Step 7 — Run the existing Stripe webhook tests** (if any) to confirm no regression: `cd apps/functions && npm test -- --testPathPattern=stripe`.
  </action>
  <verify>
    <automated>cd apps/functions &amp;&amp; npm run build</automated>
    <automated>cd apps/functions &amp;&amp; npm test -- --testPathPattern=syncTenantPlanBillingSnapshot</automated>
    <automated>cd apps/functions &amp;&amp; npm test -- --testPathPattern=stripe</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('apps/functions/src/stripe/stripeWebhook.ts','utf8'); ['export async function syncTenantPlanBillingSnapshot','SyncTenantPlanBillingSnapshotParams','subscription:','lastEventId','syncedAt'].forEach(t=>{if(!s.includes(t)){console.error('Missing:',t);process.exit(1)}}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/functions/src/stripe/stripeWebhook.ts` declares `export async function syncTenantPlanBillingSnapshot(params: SyncTenantPlanBillingSnapshotParams)` (grep: `export async function syncTenantPlanBillingSnapshot`)
    - File imports `SyncTenantPlanBillingSnapshotParams` and `SubscriptionSnapshot` from `../shared/billing-types`
    - Inside the existing `db.runTransaction()` block, the patch object includes a `subscription: { ...existingSubscription, ... }` key (grep: `subscription:\s*\{`)
    - `subscription.syncedAt`, `subscription.status`, `subscription.lastEventId` lines are present (grep on each literal)
    - The file STILL has exactly ONE `db.runTransaction(` call inside `syncTenantPlanBillingSnapshot` (no new transactions added in this function)
    - The lines `clearTenantPlanCache(tenantId)`, `tenantPlanAllowsWhatsApp(tenantId)`, and `tenantRef.update({ whatsappEnabled` are AFTER the closing `})` of `db.runTransaction(` (grep + line-number check)
    - Inside `handleSubscriptionUpdated`, `handleSubscriptionDeleted`, and `handleInvoicePaymentFailed`, NO direct `tenantRef.set(` or `tenantRef.update(` calls remain (grep on those handler bodies — find function start/end via brace counting). Each handler ends in a `syncTenantPlanBillingSnapshot(` call.
    - `cd apps/functions && npm test -- --testPathPattern=syncTenantPlanBillingSnapshot` passes (>=5 real `it(` tests, 0 failures, 0 todos remaining for the 5 behaviors listed)
    - `cd apps/functions && npm run build` exits 0
  </acceptance_criteria>
  <done>The single writer in `stripeWebhook.ts` writes top-level + `subscription.*` atomically; in-file handlers route through it; the Wave 0 test file has real assertions and passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Promote beginStripeEventProcessing to a named export (visibility-only change)</name>
  <files>apps/functions/src/stripe/stripeWebhook.ts</files>
  <read_first>
    - apps/functions/src/stripe/stripeWebhook.ts lines 1-50 (current import/export style at the top of the file — confirm whether other helpers are exported via `export function` inline or a separate `export { ... }` block)
    - apps/functions/src/stripe/stripeWebhook.ts lines 445-490 (current `beginStripeEventProcessing` declaration — confirm signature, return type, and that no other in-file caller breaks when visibility changes)
    - apps/functions/src/stripe/stripeWebhook.ts (full file — grep `beginStripeEventProcessing` to confirm all callers are in the same file; if any callsite outside this file exists today, STOP and surface — that would mean the function was already exported and the revision premise is wrong)
  </read_first>
  <action>
    **Step 1 — Verify the current declaration.** Open `apps/functions/src/stripe/stripeWebhook.ts` and locate the function. The current line should read approximately:
    ```typescript
    async function beginStripeEventProcessing(event: Stripe.Event): Promise<"skip" | "process"> {
    ```
    If the function is ALREADY exported (for example: `export async function beginStripeEventProcessing(...)`), STOP and surface — the revision premise (function is file-private) would be wrong, and Plan 05 can already import it. Do NOT proceed silently.

    **Step 2 — Change ONLY the visibility.** Add the `export` keyword in front of `async function`:
    ```typescript
    export async function beginStripeEventProcessing(event: Stripe.Event): Promise<"skip" | "process"> {
      // unchanged body — DO NOT modify the function internals
    }
    ```
    Do NOT modify:
    - The parameter list or return type
    - The function body (the existing `db.runTransaction()` and surrounding logic)
    - Any other function in the file
    - The `shouldSkipStripeEventRecord` declaration (verify it is already exported; if not, leave it as-is — Plan 05 can drive the predicate via `beginStripeEventProcessing` instead)

    **Step 3 — Confirm in-file callers still work.** Grep the file for `beginStripeEventProcessing(` to confirm all in-file callers (the webhook entry handler) still find the symbol. The named export does not change in-file callability.

    **Step 4 — Confirm no circular-import risk.** Plan 05's test file (`apps/functions/src/billing/__tests__/stripe-idempotency.test.ts`) imports from `"../../stripe/stripeWebhook"`. `stripeWebhook.ts` does not import from `__tests__/`, so no cycle is introduced.

    **Step 5 — Build and run the Stripe test suite** to confirm zero behavior change:
    ```bash
    cd apps/functions && npm run build
    cd apps/functions && npm test -- --testPathPattern=stripe
    ```
    Both must exit 0. The export change is non-functional at runtime; tests should be unchanged in pass/fail counts vs. Task 1's results.
  </action>
  <verify>
    <automated>cd apps/functions &amp;&amp; npm run build</automated>
    <automated>cd apps/functions &amp;&amp; npm test -- --testPathPattern=stripe</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('apps/functions/src/stripe/stripeWebhook.ts','utf8'); if(!/export\\s+async\\s+function\\s+beginStripeEventProcessing\\s*\\(/.test(s)){console.error('beginStripeEventProcessing is not a named export');process.exit(1)} const matches=s.match(/function\\s+beginStripeEventProcessing\\s*\\(/g)||[]; if(matches.length!==1){console.error('Expected exactly one declaration of beginStripeEventProcessing; found:',matches.length);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/functions/src/stripe/stripeWebhook.ts` contains the line `export async function beginStripeEventProcessing(event: Stripe.Event): Promise<"skip" | "process">` (grep for the regex `export\s+async\s+function\s+beginStripeEventProcessing`)
    - The function appears exactly ONCE in the file (no duplicate, no separate `export { beginStripeEventProcessing }` block needed; inline export is sufficient)
    - The function body is BYTE-IDENTICAL to its pre-edit body (verifiable by `git diff`: only the `export` keyword is added; no other lines inside the function change)
    - The function signature `(event: Stripe.Event): Promise<"skip" | "process">` is UNCHANGED
    - In-file callers (the webhook HTTP entry handler) still call `beginStripeEventProcessing(...)` without modification
    - `cd apps/functions && npm run build` exits 0
    - `cd apps/functions && npm test -- --testPathPattern=stripe` exits 0; pass/fail counts match Task 1's final state (no regression introduced by the visibility change)
  </acceptance_criteria>
  <done>`beginStripeEventProcessing` is a named export with unchanged signature and body. Plan 05's emulator replay test can now import it directly and call it twice with the same eventId, eliminating the fallback path described in the prior Plan 05 draft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe webhook -> Cloud Function | Inbound event, signature already verified upstream by HMAC; `event.id` consumed as idempotency key |
| Cloud Function -> Firestore tenant doc | Atomic write boundary; transaction prevents partial-state reads by other code paths |
| Test harness -> beginStripeEventProcessing | Plan 05 imports the now-exported function to drive replay assertions; no production runtime change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Tampering | Concurrent webhook+cron writing same tenant doc | mitigate | Single `db.runTransaction()` in `syncTenantPlanBillingSnapshot` writes top-level AND `subscription.*` atomically; merge:true preserves prior subscription.* fields. Confirmed by Test 1 + Test 5. |
| T-19-02-02 | Tampering | whatsappEnabled write race vs main transaction | accept | Pitfall 2 — derived field; eventual consistency acceptable; moving inside tx breaks correctness. Documented in objective. |
| T-19-02-03 | Information Disclosure | subscription.* fields readable via security rules | accept | Existing tenants/{id} read rules already gate by tenant membership; new fields inherit; no rule change needed in Phase 19. |
| T-19-02-04 | Repudiation | Webhook event id missing from audit trail | mitigate | `subscription.lastEventId` and `subscription.syncedAt` written on every call enable post-hoc reconciliation against Stripe Dashboard event log. |
| T-19-02-05 | Information Disclosure | beginStripeEventProcessing now exported widens module API surface | accept | Function only mutates `stripe_events/{eventId}` with predictable, idempotent semantics; no PII or secrets exposed; consumed only by tests today. Phase-19 audit narrative documents the export. |
</threat_model>

<verification>
- `cd apps/functions && npm run build` exits 0
- `cd apps/functions && npm test -- --testPathPattern=syncTenantPlanBillingSnapshot` reports >=5 real tests passing
- `cd apps/functions && npm test -- --testPathPattern=stripe` exits 0 (no regression in webhook handler tests)
- Grep confirms `subscription:` appears inside the function body and no in-file webhook handler writes tenantRef directly
- Grep confirms `export async function beginStripeEventProcessing` is present (Task 2)
</verification>

<success_criteria>
1. `syncTenantPlanBillingSnapshot` is exported, accepts `SyncTenantPlanBillingSnapshotParams`, and writes both top-level + `subscription.*` inside ONE `db.runTransaction()`
2. `handleSubscriptionUpdated` deferral path, `handleSubscriptionDeleted`, `handleInvoicePaymentFailed` route through the function (zero direct tenantRef writes inside their bodies)
3. `whatsappEnabled` write remains outside the transaction (Pitfall 2)
4. Wave 0 test scaffold replaced with >=5 passing assertions; build green
5. No top-level field is removed
6. `beginStripeEventProcessing` is a named export with unchanged signature and body, ready for Plan 05's BILL-08 emulator replay test
</success_criteria>

<output>
After completion, create `.planning/phases/19-single-writer-billing-foundation/19-02-SUMMARY.md` documenting:
- Final exported function signature for `syncTenantPlanBillingSnapshot` (parameter list)
- List of every `subscription.*` field now written by the function
- Which in-file handlers were consolidated and what they used to write directly
- Confirmation that whatsappEnabled remained outside the transaction
- Confirmation that `beginStripeEventProcessing` is now a named export with unchanged signature/body (cite the line number where `export async function beginStripeEventProcessing(...)` now lives)
- Test count + pass/fail summary
</output>
